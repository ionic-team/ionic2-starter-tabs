var npm = require('npm');
var Promise = require('bluebird');
function ensureCompiler(compiler) {
    var config = require('../package.json');
    var promise = new Promise(function (resolve, reject) {
        npm.load([config], function (er, npm) {
            npm.commands.install([compiler], resolve);
        });
    });
    return promise;
}
exports.ensureCompiler = ensureCompiler;
function rebuildLoader() {
}
exports.rebuildLoader = rebuildLoader;
//# sourceMappingURL=npm.js.map